import MiniGraph from './miniGraph/queryReduxForLists';

const ScoresSummaries = () => {

  return (
    <>
      <div className="h4">ScoresSummaries</div>
      <div>minimize the below; show only the table with list item scores. Then show table for every list.</div>
      <MiniGraph />
    </>
  );
};
export default ScoresSummaries;
