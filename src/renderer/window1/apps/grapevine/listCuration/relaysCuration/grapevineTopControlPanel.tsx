import React, { useEffect, useRef } from 'react';
import { DataSet, Network } from 'vis-network/standalone/esm/vis-network';

const GrapevineTopControlPanel = () => {
  return (
    <>
      <div className="h4">downloading ratings to redux</div>
    </>
  );
};
export default GrapevineTopControlPanel;
